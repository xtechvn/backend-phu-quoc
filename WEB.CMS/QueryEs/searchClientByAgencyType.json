{
  "endpoint": "{index_name}/_search?filter_path=hits.hits._source.email,hits.hits._source.phone,hits.hits._source.clientname,hits.hits._source.id,hits.hits._source.clienttype,hits.hits._source.userid,hits.hits._source._id,hits.hits._source.agencytype",
  "input_query": {
    "query": {
      "bool": {
        "should": [
          {
            "match_phrase": {
              "agencytype": "{keyword}"
            }
          
          }
        ]
      }
    },
    "_source": [ "email", "phone", "clientname", "id", "clienttype", "userid", "agencytype" ]
  }
}